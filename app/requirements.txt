fastapi             # Web framework for building APIs quickly and efficiently
uvicorn[standard]   # ASGI server used to run the FastAPI app
requests            # Library to make HTTP requests (used for calling external APIs like Open-Meteo)
